# TuneCat
Small discord bot used for playing music in discord calls

# Setting up your environment
You will need a text file named ```.env``` in the same directory to store your bot's token. Include only the line ```TOKEN=<YOUR TOKEN HERE>```